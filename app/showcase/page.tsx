import Link from "next/link";
import { ArrowRight, BadgeCheck, Check, Crown, Sparkles, Sun } from "lucide-react";
import { SHOWCASE_PROFILES, showcaseTiers } from "@/data/showcase-profiles";

const featureRows = ["Verified status & trust badges", "Professional company copy", "Reviews displayed", "Project case studies", "Gallery & video", "Featured search placement", "Lead tracking & analytics"];

export default function ShowcaseIndex() {
  return <main className="showcase-index"><div className="demo-bar"><span>SALES SHOWCASE</span><span>Four demonstration profiles for installer conversations</span><Link href="/">Back to directory <ArrowRight size={14} /></Link></div><nav className="showcase-nav"><Link href="/" className="brand-lockup"><span className="brand-mark"><Sun size={19} /></span><span>The Renewable <b>Directory</b></span></Link><span className="index-nav-note">A clearer way to see the value of being found.</span></nav><section className="index-hero"><span className="kicker">Installer profile showcase</span><h1>Give homeowners a reason<br /><em>to choose you.</em></h1><p>Four example profiles. One clear upgrade path. Send the right page to an installer and let the experience do the explaining.</p></section><section className="tier-comparison"><div className="tier-heading"><div><span className="kicker">Compare the packages</span><h2>From listed to first choice.</h2></div><p>Every tier is designed to make a business look more credible, more capable and easier to contact.</p></div><div className="tier-cards">{showcaseTiers.map((tier) => { const p = SHOWCASE_PROFILES[tier]; return <Link href={`/showcase/${tier}`} key={tier} className={`tier-card tier-card-${tier}`}>{p.priority && <span className="tier-ribbon"><Crown size={13} /> Highest visibility</span>}<div className="tier-card-top"><span className="tier-icon">{tier === "priority" ? <Crown /> : tier === "featured" ? <Sparkles /> : tier === "verified" ? <BadgeCheck /> : <Sun />}</span><span>{p.label}</span></div><h3>{p.name}</h3><p>{p.description}</p><div className="tier-price"><b>{p.price}</b><span>per month</span></div><div className="tier-preview"><img src={p.hero} alt="" /></div><span className="tier-link">View showcase <ArrowRight size={16} /></span></Link>})}</div></section><section className="comparison-table-section"><div className="comparison-head"><div><span className="kicker">The value, at a glance</span><h2>Every upgrade adds proof and performance.</h2></div><Link href="/apply" className="green-button">Talk to the team <ArrowRight size={16} /></Link></div><div className="comparison-table"><div className="comparison-row comparison-row-head"><span>Included</span>{showcaseTiers.map((tier) => <b key={tier}>{SHOWCASE_PROFILES[tier].label}</b>)}</div>{featureRows.map((feature, i) => <div className="comparison-row" key={feature}><span>{feature}</span>{showcaseTiers.map((tier) => <span key={tier}>{(i < 2 || (i === 2 && tier !== "free") || (i === 3 && tier !== "free") || (i === 4 && (tier === "featured" || tier === "priority")) || (i > 4 && tier === "priority")) ? <Check size={17} /> : "—"}</span>)}</div>)}</div></section><section className="index-bottom"><span className="kicker light">Ready to show the difference?</span><h2>Start with the profile your next advertiser wants.</h2><Link href="/showcase/priority" className="green-button">View Priority Lead Partner <ArrowRight size={16} /></Link></section><footer className="showcase-footer"><Link href="/" className="brand-lockup"><span className="brand-mark"><Sun size={17} /></span><span>The Renewable <b>Directory</b></span></Link><span>Demonstration profiles · Not real installers</span></footer></main>;
}

